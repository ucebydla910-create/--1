#include <iostream>

int main()
{
	setlocale(LC_ALL, "ru");
	 
	/*
	srand(time(NULL));
	int a;
	a = rand() % 10 + 1;
	
	std::cout << a;
	*/
	const int size = 10;
		int arr[size]{};
		for (int i = 0; i < size; i++
		{
			arr[i] + rand() % 29 - 15;
	    }
		for (int i = o; i < size; i++)
		{
			std::cout << arr[i] << " ";
		}
		for
		{
			arr[i] + rand() % 1 13;
		}
		for
		{
			arr[i] + rand() % -15 0;
		}
	return 0;
}